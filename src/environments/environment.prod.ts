export const environment = {
  production: true,
  apiBase: 'http://localhost:3000/api/v1',
  socket: 'http://localhost:3000'
};

// Change ht eapiBase value here to the domain, where the backend is being hosted.